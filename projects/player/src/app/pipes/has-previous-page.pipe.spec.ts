import { HasPreviousPagePipe } from './has-previous-page.pipe';

describe('HasPreviousPagePipe', () => {
  it('create an instance', () => {
    const pipe = new HasPreviousPagePipe();
    expect(pipe).toBeTruthy();
  });
});
