import { IsValidPagePipe } from './is-valid-page.pipe';

describe('IsValidPagePipe', () => {
  it('create an instance', () => {
    const pipe = new IsValidPagePipe();
    expect(pipe).toBeTruthy();
  });
});
